# DSH Workbench Layout 项目开发提示词

## 项目目标

- 本项目是 DeepSeek Harness Web 的工作台布局插件。
- 新增能力必须自然融入 DSH，视觉、交互和动效不能呈现为另一套产品。
- 修改应解决根因，不通过堆叠覆盖、延时补丁或重复实现掩盖问题。

## 官方实现优先

1. 修改界面前，先检查与目标功能最接近的 DSH 官方源码和现有组件，确认其 DOM、状态、尺寸、间距、颜色、动效及响应式规则。
2. 优先复用 DSH 官方组件、图标、插槽、布局轨道、CSS 变量和设计令牌。
3. 官方没有对应实现时，才允许新增本地组件；新增部分必须遵循官方的命名、16px 图标规格、排版密度、圆角、色阶和交互反馈。
4. 不用通用后台模板、其他组件库默认样式或个人偏好替代 DSH 的设计依据。
5. 不移动由 React 持有的官方 DOM 节点；需要与官方页面协作时，优先使用公开插槽、稳定属性和最小范围的布局标记。
6. 样式选择器必须限定在插件稳定标记下，禁止使用可能污染整个页面的宽泛选择器。

## 视觉与交互

- 收起侧栏遵循 36px 圆形点击区和 12px 纵向节奏，图标优先使用官方 16px/18px 规格。
- 颜色必须优先来自 DSH 设计令牌，同时验证浅色和深色主题；禁止为了单一截图随意写死颜色。
- 动画优先沿用官方时长、缓动和阶段切换方式，避免闪烁、二次动画和中间状态重排。
- 窄栏、空内容、无会话、无工作区、多标签和长文本都是正式状态，不能只保证默认宽度下可用。
- 图标必须语义准确；不得用代码、数据等近似图标冒充文件、目录或其他明确对象。
- 悬浮提示、焦点、禁用态、选中态和键盘操作必须同时可用，不能只优化鼠标静态外观。

## 代码组织

- 保持高内聚低耦合，一个文件只承担清晰、单一的职责。
- 复杂布局探测、状态控制、视图组件和图标资源应拆分到独立模块，不在一个组件内持续堆叠。
- 删除被替代的旧实现、死分支和无效样式，不保留并行兼容版本。
- 修复问题前先确认根因，并为边界条件补充可复现测试。

## 配套交付

- 每次代码修改必须同步更新相关日志、中文与英文文档以及受影响测试。
- 完成独立修改点后立即创建中文 Git 提交，提交说明简洁描述实际结果。
- 复杂改动实施前先给出问题理解、执行步骤、影响范围和风险，等待用户确认。
- 交付前至少运行类型检查、相关测试、完整测试、正式构建和客户端 bundle 检查。

## 隐私与发布

- 源码、测试、文档、日志和提交信息中不得出现个人绝对路径、本机账户名、令牌、密钥或其他本机隐私信息；公开的软件包作用域与仓库标识不属于本机隐私。
- 示例只使用通用的工作区 id、相对路径和匿名数据。
- 未经用户明确要求，不执行 npm 发布、Git 推送、远程仓库修改或版本号变更。
